import React from 'react';
import styles from './SectionReviews.module.css';

const SectionReviews = () => {
  //Aqui você faz a seção de comentarios do clientes
  //Recomendo para cada comentario criar um componente, ai pelo props você passa as info
  //Só para exemplo usa aquela api do quyntas (https://randomuser.me), traz a info e usa a imagem/nome
  return <div>SectionReviews</div>;
};

export default SectionReviews;
